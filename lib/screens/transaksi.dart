import 'package:flutter/material.dart';


class UserTransaksi extends StatelessWidget {
  const UserTransaksi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Transaksi',
        style: TextStyle(fontSize: 60),
      ),
    );
  }
}